# Prevent npm from creating a package-lock.json at the monorepo root.
# The root package.json only orchestrates dev commands (concurrently);
# it is not a real workspace root. A lockfile here causes Next.js/Turbopack
# to walk up past frontend/frontend and treat the repo root as the project
# filesystem, breaking Turbopack HMR subscriptions.
package-lock=false
